{
  "name": "booking-api",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "ln -sfn ../../.env .env",
    "dev": "npx wrangler dev",
    "secrets": "npx wrangler secret bulk ../../.env",
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260811.1",
    "wrangler": "^4.123.0"
  }
}
